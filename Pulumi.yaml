name: syncreeper
runtime:
    name: nodejs
    options:
        typescript: false
description: Sync GitHub repos across devices via Syncthing on a secured VPS
main: packages/host/dist
config:
    pulumi:tags:
        value:
            pulumi:template: typescript

/**
 * @syncreeper/node-passthrough
 *
 * Node.js traffic passthrough proxy for SyncReeper.
 *
 * This package runs on the VPS and forwards incoming traffic through a
 * WireGuard tunnel to the host machine. The host has a private IP and
 * the VPS has the public IP, so the VPS acts as a reverse proxy.
 *
 * TODO: Implement proxy logic
 */

console.log("@syncreeper/node-passthrough - not yet implemented");
